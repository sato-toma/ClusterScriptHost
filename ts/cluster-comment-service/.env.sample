# Database (Vercel Postgres / Neon)
# Vercel DashboardのStorageタブ、または .env.local からコピーした値を設定してください
POSTGRES_PRISMA_URL="postgres://default:password@ep-example.region.aws.neon.tech:5432/verceldb?sslmode=require&pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgres://default:password@ep-example.region.aws.neon.tech:5432/verceldb?sslmode=require"

# NextAuth Configuration
# 本番環境ではランダムな文字列を設定 (例: `openssl rand -base64 32` で生成)
NEXTAUTH_SECRET="changeme_in_production"
NEXTAUTH_URL="http://localhost:3000"

# OAuth Providers (必要に応じて追加)
# GitHub, GoogleなどのClient ID/Secret
GITHUB_ID=""
GITHUB_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
